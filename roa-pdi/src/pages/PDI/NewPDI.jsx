import { Box, Typography, Card, CardContent } from '@mui/material';

// Phase 2 implementation — scaffold only
export default function NewPDI() {
  return (
    <Box>
      <Typography variant="h5" gutterBottom>Create New PDI</Typography>
      <Card>
        <CardContent>
          <Typography variant="body2" color="text.secondary">
            PDI creation form — Phase 2.
          </Typography>
        </CardContent>
      </Card>
    </Box>
  );
}
